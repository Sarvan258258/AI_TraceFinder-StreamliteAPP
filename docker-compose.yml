version: '3.8'

services:
  ai-tracefinder:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./models:/app/models
      - ./demo_images:/app/demo_images
    environment:
      - DEMO_MODE=false
    restart: unless-stopped